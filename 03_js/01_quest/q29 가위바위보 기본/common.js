/*
    공통함수들을 모아놓음
*/
function dw(str){    // var str; var str = "고양이";
    document.write(str); // document 를 편하게 쓰기 위한 함수
} // dw("고양이") 라 치면 고양이가 뜬다.
function br(){ // br을 함수로 만들어 편하게 줄바꿈해준다.
    document.write("<br>");
}
function hr(){
    document.write("<hr>");
}